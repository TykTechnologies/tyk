component: gateway_control_api
no_interface:
    declared: true
    reason: The gateway control API proof slice covers local helper behavior and tested control API handler responses only; TLS handshake enforcement, runtime certificate authentication decisions, upstream TLS validation, certificate storage durability, dashboard behavior, network transport behavior, and broader final client-visible behavior are outside this component boundary.
variables:
    - name: gateway_control_api_operation_terminal
      type: bool
      direction: output
      description: 'The local gateway control API operation reaches a terminal observable result: success or error status object, JSON response body, JSON export body or delegated error response, method-not-allowed response, secure/cache-control wrapper headers, accepted static mTLS binding validation, explicit certificate-binding validation error, or new-certificate delta result.'
