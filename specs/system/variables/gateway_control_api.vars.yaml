component: gateway_control_api
no_interface:
    declared: true
    reason: The gateway control API proof slice covers local helper behavior, tested control API handler responses, and local reload callback scheduling only; TLS handshake enforcement, runtime certificate authentication decisions, upstream TLS validation, certificate storage durability, reload durability, dashboard behavior, network transport behavior, OAS schema semantic correctness, and broader final client-visible behavior are outside this component boundary.
variables:
    - name: gateway_control_api_operation_terminal
      type: bool
      direction: output
      description: 'The local gateway control API operation reaches a terminal observable result: success or error status object, JSON response body, JSON export body or delegated error response, method-not-allowed response, secure/cache-control wrapper headers, allowed-method dispatch result, API/OAS route-dispatch wrapper result, reload handler status or callback scheduling result, organization API lookup result, API ID listing result, API definition inventory list/get result, OAS inventory list/get result with public extension shaping, API add/update/delete status result, local filesystem write status result, OAuth client management create/list/detail/API-membership/update/rotate/token-list/refresh-invalidation status result, accepted static mTLS binding validation, explicit certificate-binding validation error, or new-certificate delta result.'
