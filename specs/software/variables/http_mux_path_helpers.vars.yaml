component: http_mux_path_helpers
variables:
    - name: http_mux_path_helpers_operation_terminal
      type: bool
      direction: output
      description: The http mux path helpers implementation operation reaches a terminal local result for the focused tested inputs, such as returning the expected value, selecting the expected local branch, surfacing an explicit local error, or preserving the documented local fallback behavior.
