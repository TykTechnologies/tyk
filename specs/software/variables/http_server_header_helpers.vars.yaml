component: http_server_header_helpers
variables:
    - name: http_connection_watcher_determined
      type: bool
      direction: output
      description: Connection watcher construction, explicit count deltas, connection-state count deltas, ignored non-counting states, and count reporting produce deterministic local implementation results for the focused tested inputs.
    - name: http_basic_auth_header_determined
      type: bool
      direction: output
      description: Basic Authorization header construction from username and password pairs produces deterministic local implementation results for the focused tested inputs.
