component: gateway_cipher_suite_selection
variables:
    - name: gateway_cipher_suite_selection_operation_terminal
      type: bool
      direction: output
      description: The gateway cipher suite selection implementation operation reaches a terminal local result for the focused tested inputs, such as returning the expected value, selecting the expected local branch, surfacing an explicit local error, or preserving the documented local fallback behavior.
