component: external_http_client
variables:
    - name: external_http_client_operation_terminal
      type: bool
      direction: output
      description: The external http client implementation operation reaches a terminal local result for the focused tested inputs, such as returning the expected value, selecting the expected local branch, surfacing an explicit local error, or preserving the documented local fallback behavior.
