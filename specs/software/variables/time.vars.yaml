component: time
no_interface:
    declared: true
    reason: 'The time package is an in-process helper for readable duration serialization, parsing, and conversion; it does not define a cross-component runtime interface boundary.'
variables:
    - name: duration_operation_requested
      type: bool
      direction: input
      description: A caller requests readable duration JSON serialization, JSON parsing, shorthand formatting, or unit conversion.

    - name: duration_operation_result_returned
      type: bool
      direction: output
      description: The duration helper returns the expected JSON string, parsed duration, conversion value, zero fallback, or explicit malformed JSON error.
