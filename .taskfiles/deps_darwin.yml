version: '3'

tasks:
  tracetest:
    desc: "install tracetest v0.11.16 on darwin"
    status:
      - type tracetest
    cmds:
      - brew install kubeshop/tracetest/tracetest@v0.11.16
      - tracetest configure -g --endpoint http://localhost:11633
