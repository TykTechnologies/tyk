fixtures:
- name: ServiceDefinition migrations
  tests:
    - input:
        proxy:
          service_discovery:
            use_discovery_service: true
            cache_disabled: false
            cache_timeout: 10
      output:
        x-tyk-api-gateway:
          upstream:
            serviceDiscovery:
              enabled: true
