# Generated by: tyk-ci/wf-gen
# Generated on: Wed 28 Apr 12:13:29 UTC 2021

# Generation commands:
# ./pr.zsh -title releng: [TD-309] multiarch manifests for Hub and Cloudsmith -branch releng/ecr
# m4 -E -DxREPO=tyk

FROM debian:buster-slim
ARG TARGETARCH

RUN apt-get update \
 && apt-get dist-upgrade -y ca-certificates \
 && apt-get autoremove -y

COPY *${TARGETARCH}.deb /
RUN dpkg -i /tyk-gateway*${TARGETARCH}.deb

ARG PORTS

EXPOSE $PORTS

WORKDIR /opt/tyk-gateway/

ENTRYPOINT ["/opt/tyk-gateway/tyk" ]
CMD [ "--conf=/opt/tyk-gateway/tyk.conf" ]
