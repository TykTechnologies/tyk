# Generated by: tyk-ci/wf-gen
# Generated on: Wed 24 Feb 05:43:24 UTC 2021

# Generation commands:
# ./pr.zsh -title fix release #2 -branch goreleaser/hybrid -repos tyk,tyk-analytics -p
# m4 -E -DxREPO=tyk

FROM debian:buster-slim
RUN apt-get update \
 && apt-get dist-upgrade -y ca-certificates \
 && apt-get autoremove -y

COPY *.deb /
RUN dpkg -i /*.deb && rm /*.deb

ARG PORTS

EXPOSE $PORTS

ENTRYPOINT ["/opt/tyk-gateway/tyk" ]
CMD [ "--conf=/opt/tyk-gateway/tyk.conf" ]
