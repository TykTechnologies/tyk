# Generated by: tyk-ci/wf-gen
# Generated on: Wed 17 Feb 17:51:15 UTC 2021

# Generation commands:
# ./pr.zsh -title use nfpm for std docker #2 -branch goreleaser/more -p
# m4 -E -DxREPO=tyk

FROM debian:buster-slim
RUN apt-get update \
 && apt-get dist-upgrade -y ca-certificates \
 && apt-get autoremove -y

COPY *.deb /
RUN dpkg -i /*.deb && ln -s /opt/tyk /opt/tyk-gateway && rm /*.deb

ARG PORTS

EXPOSE $PORTS

ENTRYPOINT ["/opt/tyk-gateway/tyk" ]
CMD [ "--conf=/opt/tyk-gateway/tyk.conf" ]
