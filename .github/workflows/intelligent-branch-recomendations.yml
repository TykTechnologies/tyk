name: PR Merge Suggestions
on:
  pull_request:
    types:
      - opened
      - synchronize
      - reopened
      - ready_for_review

permissions:
  pull-requests: write
  contents: read

jobs:
  branch-suggestions:
    uses: TykTechnologies/github-actions/.github/workflows/branch-suggestion.yml@production
    secrets:
      JIRA_READ_AUTH: ${{ secrets.JIRA_READ_AUTH }}
      JIRA_BASE_URL: ${{ secrets.JIRA_BASE_URL }}
