# Generated by: tyk-ci/wf-gen
# Generated on: Thu  6 May 07:58:07 UTC 2021

# Generation commands:
# ./meta.zsh 
# m4 -E -DxSRC_BRANCH='release-2.9' -DxRELEASE_BRANCHES='release-2.9.3.2' -DxAUTO_FILES='.goreleaser.yml Dockerfile.std Dockerfile.slim aws/byol.pkr.hcl .github/workflows/release.yml .github/workflows/del-env.yml integration/terraform/outputs.tf .github/dependabot.yml'

# This workflow will silently ignore FILES that are missing in master
# Most of the time this is what we want, older repos may not have some files

name: Sync automation

on:
  push:
    branches:
      - release-2.9
    paths:
      - .github/workflows/*
      - integration/*
      - .goreleaser.yml

env:
  FILES: .goreleaser.yml Dockerfile.std Dockerfile.slim aws/byol.pkr.hcl .github/workflows/release.yml .github/workflows/del-env.yml integration/terraform/outputs.tf .github/dependabot.yml
  
jobs:
  sync:
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        branch: [ release-2.9.3.2 ]
          
    steps:
      - uses: actions/checkout@v2
        with:
          ref: ${{matrix.branch}}

      - name: sync ${{matrix.branch}} from release-2.9
        run: |
          git fetch origin release-2.9
          git config --local user.email "wf-gen@tyk-ci"
          git config --local user.name "Bender"
          for f in $FILES; do 
            git checkout --theirs origin/release-2.9 -- $f || true
          done
          git commit -a -m "[CI] Syncing release automation from release-2.9"
          git push origin
          echo "::debug::${FILES} syncd for ${{matrix.branch}}"
