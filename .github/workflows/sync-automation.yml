# Generated by: tyk-ci/wf-gen
# Generated on: Wednesday 17 March 2021 01:11:52 PM UTC

# Generation commands:
# ./pr.zsh -branch goreleaser/fixes -title rel-eng: osx tarballs and other fixes
# m4 -E -DxRELEASE_BRANCHES='release-3, release-3-lts, release-3.2' -DxAUTO_FILES='.goreleaser.yml Dockerfile.std Dockerfile.slim aws/byol.pkr.hcl .github/workflows/release.yml .github/workflows/del-env.yml integration/terraform/outputs.tf'

# This workflow will silently ignore FILES that are missing in master
# Most of the time this is what we want, older repos may not have some files

name: Sync automation

on:
  push:
    branches:
      - master
    paths:
      - .github/workflows/*
      - integration/*
      - .goreleaser.yml

env:
  FILES: .goreleaser.yml Dockerfile.std Dockerfile.slim aws/byol.pkr.hcl .github/workflows/release.yml .github/workflows/del-env.yml integration/terraform/outputs.tf
  
jobs:
  sync:
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        branch: [ release-3.2 ]
          
    steps:
      - uses: actions/checkout@v2
        with:
          ref: ${{matrix.branch}}

      - name: sync ${{matrix.branch}} from master
        run: |
          git fetch origin master
          git config --local user.email "wf-gen@tyk-ci"
          git config --local user.name "Bender"
          for f in $FILES; do 
            git checkout --theirs origin/master -- $f || true
          done
          git commit -a -m "[CI] Syncing release automation from master"
          git push origin
          echo "::warning::${FILES} syncd for ${{matrix.branch}}"
