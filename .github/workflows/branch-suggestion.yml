name: intelligent branch merge suggestion
on:
  pull_request:
    types:
      - opened
      - synchronize
      - reopened

permissions:
  pull-requests: write
  contents: read

jobs:
  branch-suggestions:
    uses: TykTechnologies/github-actions/.github/workflows/branch-suggestion.yml@eab54c1cf34cf83b95a1896d3ea79344be993fc9
    secrets:
      JIRA_TOKEN: ${{ secrets.JIRA_TOKEN }}