name: intelligent branch merge suggestion
on:
  pull_request:
    types:
      - opened
      - synchronize
      - reopened

permissions:
  pull-requests: write
  contents: read

jobs:
  branch-suggestions:
    uses: TykTechnologies/github-actions/.github/workflows/branch-suggestion.yml@eeabfe32944cda7aeb58046c877e65a0791d21ca
    secrets:
      JIRA_TOKEN: ${{ secrets.JIRA_TOKEN }}