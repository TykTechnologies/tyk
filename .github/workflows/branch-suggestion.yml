name: intelligent branch merge suggestion.
on:
  pull_request:
    types:
      - opened
      - synchronize
      - reopened

permissions:
  pull-requests: write
  contents: read

jobs:
  branch-suggestions:
    uses: TykTechnologies/github-actions/.github/workflows/branch-suggestion.yml@6b28e0d68dbfc2672aab9c4893b8b7feb6c1585d
    secrets:
      JIRA_TOKEN: ${{ secrets.JIRA_TOKEN }}