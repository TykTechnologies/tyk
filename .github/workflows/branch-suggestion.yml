name: intelligent branch merge suggestion
on:
  pull_request:
    types:
      - opened
      - synchronize
      - reopened

permissions:
  pull-requests: write
  contents: read

jobs:
  branch-suggestions:
    uses: TykTechnologies/github-actions/.github/workflows/branch-suggestion.yml@7662a0d393a618ccab147f1f0eeba49dbc638f4a
    secrets:
      JIRA_TOKEN: ${{ secrets.JIRA_TOKEN }}