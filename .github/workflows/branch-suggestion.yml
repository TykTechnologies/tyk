name: intelligent branch merge suggestion
on:
  pull_request:
    types:
      - opened
      - synchronize
      - reopened

permissions:
  pull-requests: write
  contents: read

jobs:
  branch-suggestions:
    uses: TykTechnologies/github-actions/.github/workflows/branch-suggestion.yml@fd4f95926f78bdec2aca5e70b5beb54f885fa7f7
    secrets:
      JIRA_TOKEN: ${{ secrets.JIRA_TOKEN }}