name: intelligent branch merge suggestion.
on:
  pull_request:
    types:
      - opened
      - synchronize
      - reopened

permissions:
  pull-requests: write
  contents: read

jobs:
  branch-suggestions:
    uses: TykTechnologies/github-actions/.github/workflows/branch-suggestion.yml@6a6661959069b11679c5f7e29bce0c0de19f6a89
    secrets:
      JIRA_TOKEN: ${{ secrets.JIRA_TOKEN }}