name: Release bot

on:
  issue_comment:
    types: [created]

jobs:
  release_bot:
    uses: TykTechnologies/github-actions/.github/workflows/release-bot.yaml@92b66e740444aba67fdbc96c07c69e1c6dd6f32d  # main
    secrets:
      APP_ID: ${{ secrets.PROBE_APP_ID }}
      APP_PRIVATE_KEY: ${{ secrets.PROBE_APP_PRIVATE_KEY }}
      APP_INSTALLATION_ID: ${{ secrets.PROBE_APP_INSTALLATION_ID }}
