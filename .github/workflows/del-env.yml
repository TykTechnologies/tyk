# Generated by: tyk-ci/wf-gen
# Generated on: Fri 18 Jun 09:02:35 UTC 2021

# Generation commands:
# ./pr.zsh -title [TD-351] Sync releng with release branches -branch sync/releng3.2 -base release-3.2 -repos tyk
# m4 -E -DxREPO=tyk

name: Retiring dev env

on:
  delete:
    branches:
      - feature/*
      - integration/*

jobs:
  retire:
    runs-on: ubuntu-latest

    steps:
      - name: Tell gromit about deleted branch
        run: |
            curl -fsSL -H "Authorization: ${{secrets.GROMIT_TOKEN}}" "https://domu-kun.cloud.tyk.io/gromit/env/${GITHUB_REF##*/}" \
                 -X DELETE
