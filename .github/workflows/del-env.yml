

# Generated by: tyk-ci/wf-gen
# Generated on: Thursday 03 February 2022 05:17:01 PM UTC

# Generation commands:
# ./pr.zsh -p -repos tyk -title Sync from templates -branch exp/el7-builds -base exp/el7-builds
# m4 -E -DxREPO=tyk

name: Retiring dev env

on:
  delete:
    branches:
      - feature/*
      - integration/*

jobs:
  retire:
    runs-on: ubuntu-latest

    steps:
      - name: Tell gromit about deleted branch
        run: |
            curl -fsSL -H "Authorization: ${{secrets.GROMIT_TOKEN}}" "https://domu-kun.cloud.tyk.io/gromit/env/${GITHUB_REF##*/}" \
                 -X DELETE
