# Generated by: tyk-ci/wf-gen
# Generated on: Fri  9 Apr 13:35:27 UTC 2021

# Generation commands:
# ./pr.zsh -title releng: provide freshly built asset for AMIs -branch releng/docker -base releng/docker -p
# m4 -E -DxREPO=tyk

name: Retiring dev env

on:
  delete:
    branches:
      - feature/*
      - integration/*

jobs:
  retire:
    runs-on: ubuntu-latest

    steps:
      - name: Tell gromit about deleted branch
        run: |
            curl -fsSL -H "Authorization: ${{secrets.GROMIT_TOKEN}}" "https://domu-kun.cloud.tyk.io/gromit/env/${GITHUB_REF##*/}" \
                 -X DELETE
