# Generated by: tyk-ci/wf-gen
# Generated on: Sun 25 Apr 10:01:42 UTC 2021

# Generation commands:
# ./pr.zsh -title updated release engineering -branch release-3-lts -base release-3-lts -repos tyk,tyk-analytics -p
# m4 -E -DxREPO=tyk

name: Retiring dev env

on:
  delete:
    branches:
      - feature/*
      - integration/*

jobs:
  retire:
    runs-on: ubuntu-latest

    steps:
      - name: Tell gromit about deleted branch
        run: |
            curl -fsSL -H "Authorization: ${{secrets.GROMIT_TOKEN}}" "https://domu-kun.cloud.tyk.io/gromit/env/${GITHUB_REF##*/}" \
                 -X DELETE
