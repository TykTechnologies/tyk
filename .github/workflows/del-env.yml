# Generated by: tyk-ci/wf-gen
# Generated on: Wed 28 Apr 19:05:17 UTC 2021

# Generation commands:
# ./pr.zsh -title local unlock-agent for all -branch releng/arm64 -base releng/arm64 -p
# m4 -E -DxREPO=tyk

name: Retiring dev env

on:
  delete:
    branches:
      - feature/*
      - integration/*

jobs:
  retire:
    runs-on: ubuntu-latest

    steps:
      - name: Tell gromit about deleted branch
        run: |
            curl -fsSL -H "Authorization: ${{secrets.GROMIT_TOKEN}}" "https://domu-kun.cloud.tyk.io/gromit/env/${GITHUB_REF##*/}" \
                 -X DELETE
