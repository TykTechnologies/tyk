

# Generated by: tyk-ci/wf-gen
# Generated on: Tuesday 08 February 2022 09:15:51 AM UTC

# Generation commands:
# ./pr.zsh -repos tyk -base release-4-lts -branch tt-4363-port -base release-4-lts -title Sync from templates- el7 changes -p
# m4 -E -DxREPO=tyk

name: Retiring dev env

on:
  delete:
    branches:
      - feature/*
      - integration/*

jobs:
  retire:
    runs-on: ubuntu-latest

    steps:
      - name: Tell gromit about deleted branch
        run: |
            curl -fsSL -H "Authorization: ${{secrets.GROMIT_TOKEN}}" "https://domu-kun.cloud.tyk.io/gromit/env/${GITHUB_REF##*/}" \
                 -X DELETE
