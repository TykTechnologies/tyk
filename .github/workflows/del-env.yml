# Generated by: tyk-ci/wf-gen
# Generated on: Tue 29 Jun 15:31:17 UTC 2021

# Generation commands:
# ./pr.zsh -title publishing depends on smoke-tests -branch releng/master -base releng/master -p
# m4 -E -DxREPO=tyk

name: Retiring dev env

on:
  delete:
    branches:
      - feature/*
      - integration/*

jobs:
  retire:
    runs-on: ubuntu-latest

    steps:
      - name: Tell gromit about deleted branch
        run: |
            curl -fsSL -H "Authorization: ${{secrets.GROMIT_TOKEN}}" "https://domu-kun.cloud.tyk.io/gromit/env/${GITHUB_REF##*/}" \
                 -X DELETE
