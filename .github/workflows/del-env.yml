# Generated by: tyk-ci/wf-gen
# Generated on: Thu  6 May 04:48:21 UTC 2021

# Generation commands:
# ./pr.zsh -title releng: [TD-309] cloudsmith arm64 -branch releng/arm64
# m4 -E -DxREPO=tyk

name: Retiring dev env

on:
  delete:
    branches:
      - feature/*
      - integration/*

jobs:
  retire:
    runs-on: ubuntu-latest

    steps:
      - name: Tell gromit about deleted branch
        run: |
            curl -fsSL -H "Authorization: ${{secrets.GROMIT_TOKEN}}" "https://domu-kun.cloud.tyk.io/gromit/env/${GITHUB_REF##*/}" \
                 -X DELETE
