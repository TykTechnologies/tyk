# Generated by: tyk-ci/wf-gen
# Generated on: Tue Jan 25 15:21:50 UTC 2022

# Generation commands:
# ./pr.zsh -base release-3.0.9 -branch TD-845/Fix-vendor -title Sync TD-845/Fix-vendor with m4 releng templates -repos tyk
# m4 -E -DxREPO=tyk

name: Retiring dev env

on:
  delete:
    branches:
      - feature/*
      - integration/*

jobs:
  retire:
    runs-on: ubuntu-latest

    steps:
      - name: Tell gromit about deleted branch
        run: |
            curl -fsSL -H "Authorization: ${{secrets.GROMIT_TOKEN}}" "https://domu-kun.cloud.tyk.io/gromit/env/${GITHUB_REF##*/}" \
                 -X DELETE
