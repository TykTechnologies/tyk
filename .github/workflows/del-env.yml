# Generated by: tyk-ci/wf-gen
# Generated on: Thu 18 Mar 11:14:15 UTC 2021

# Generation commands:
# ./pr.zsh -title Push docker images to Cloudsmith -branch cloudsmith-release -repos tyk,tyk-pump
# m4 -E -DxREPO=tyk

name: Retiring dev env

on:
  delete:
    branches:
      - feature/*
      - integration/*

jobs:
  retire:
    runs-on: ubuntu-latest

    steps:
      - name: Tell gromit about deleted branch
        run: |
            curl -fsSL -H "Authorization: ${{secrets.GROMIT_TOKEN}}" "https://domu-kun.cloud.tyk.io/gromit/env/${GITHUB_REF##*/}" \
                 -X DELETE
