# Generated by: tyk-ci/wf-gen
# Generated on: Fri May 28 17:31:18 UTC 2021

# Generation commands:
# ./pr.zsh -branch td351/cloudsmith-manifests -title [TD-351] Remove cloudsmith manifest/image upload
# m4 -E -DxREPO=tyk

name: Retiring dev env

on:
  delete:
    branches:
      - feature/*
      - integration/*

jobs:
  retire:
    runs-on: ubuntu-latest

    steps:
      - name: Tell gromit about deleted branch
        run: |
            curl -fsSL -H "Authorization: ${{secrets.GROMIT_TOKEN}}" "https://domu-kun.cloud.tyk.io/gromit/env/${GITHUB_REF##*/}" \
                 -X DELETE
