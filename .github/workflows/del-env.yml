# Generated by: tyk-ci/wf-gen
# Generated on: Tue  6 Jul 13:00:53 UTC 2021

# Generation commands:
# ./pr.zsh -title releng: sync to release-3.2 -branch release-3.2 -base release-3.2 -repos tyk -p
# m4 -E -DxREPO=tyk

name: Retiring dev env

on:
  delete:
    branches:
      - feature/*
      - integration/*

jobs:
  retire:
    runs-on: ubuntu-latest

    steps:
      - name: Tell gromit about deleted branch
        run: |
            curl -fsSL -H "Authorization: ${{secrets.GROMIT_TOKEN}}" "https://domu-kun.cloud.tyk.io/gromit/env/${GITHUB_REF##*/}" \
                 -X DELETE
