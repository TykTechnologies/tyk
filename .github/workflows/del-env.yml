

# Generated by: tyk-ci/wf-gen
# Generated on: Monday 07 February 2022 05:58:01 AM UTC

# Generation commands:
# ./pr.zsh -p -repos tyk -title Sync from templates -base exp/el7-builds -branch exp/el7-builds
# m4 -E -DxREPO=tyk

name: Retiring dev env

on:
  delete:
    branches:
      - feature/*
      - integration/*

jobs:
  retire:
    runs-on: ubuntu-latest

    steps:
      - name: Tell gromit about deleted branch
        run: |
            curl -fsSL -H "Authorization: ${{secrets.GROMIT_TOKEN}}" "https://domu-kun.cloud.tyk.io/gromit/env/${GITHUB_REF##*/}" \
                 -X DELETE
