# Generated by: tyk-ci/wf-gen
# Generated on: Wed  7 Apr 09:17:40 UTC 2021

# Generation commands:
# ./pr.zsh -title Do not make unnecessary overlayfs in std docker images -branch releng/mounts
# m4 -E -DxREPO=tyk

name: Retiring dev env

on:
  delete:
    branches:
      - feature/*
      - integration/*

jobs:
  retire:
    runs-on: ubuntu-latest

    steps:
      - name: Tell gromit about deleted branch
        run: |
            curl -fsSL -H "Authorization: ${{secrets.GROMIT_TOKEN}}" "https://domu-kun.cloud.tyk.io/gromit/env/${GITHUB_REF##*/}" \
                 -X DELETE
