# Generated by: tyk-ci/wf-gen
# Generated on: Thu  8 Apr 04:23:27 UTC 2021

# Generation commands:
# ./pr.zsh -title Plugin compiler to build from tag -repos tyk -branch releng/docker -base releng/docker -p
# m4 -E -DxREPO=tyk

name: Retiring dev env

on:
  delete:
    branches:
      - feature/*
      - integration/*

jobs:
  retire:
    runs-on: ubuntu-latest

    steps:
      - name: Tell gromit about deleted branch
        run: |
            curl -fsSL -H "Authorization: ${{secrets.GROMIT_TOKEN}}" "https://domu-kun.cloud.tyk.io/gromit/env/${GITHUB_REF##*/}" \
                 -X DELETE
